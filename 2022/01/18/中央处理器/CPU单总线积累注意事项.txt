1.一般写成ADD (R0), R1这种形式的操作数第一个操作数为目的操作数，第二个操作数为源操作数
一般进行计算后再次存储于目的操作数，也就是第一个操作数中。

2.取指周期可以和间址周期与执行周期区分开
但是不同教材定义的周期不一样，通常不会要求区分
取指周期以外的周期，都统称为执行周期

3.如果操作数不是（R0） R1这类，而是前面带#号
如果# +　数字的话则那个数字就是操作数本身
如果是R1这种，就代表操作数的地址就是在R1这个寄存器内